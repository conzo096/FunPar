package c3 
 
import org.jcsp.lang.*

class Negator implements CSProcess {
  
  def ChannelInput inChannel
  def ChannelOutput outChannel
  
  void run () {
    while (true) {
      //output the negative of the input value
		def num = inChannel.read()
		outChannel.write( -1 * num)
    }
  }
}
